# Tasty Library
A open-source Delicious Library alternative using python.

## Installation

## The command `tl`

`tl` stands for **t**asty **l**ibrary.

### `tl setup`
`tl setup` is used for setting up new profiles.
